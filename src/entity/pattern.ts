export const searchPattern: RegExp = /^.{2,}$/;
export const mobilePattern: RegExp = /^09[0-9]{9}$/;
export const namePattern: RegExp = /^.{2,}\s.{2,}$/;
export const addressPattern: RegExp = /^.{10,}$/;
export const otpPattern: RegExp = /^.{4}$/;
export const emailPattern: RegExp = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;
export const messagePattern: RegExp = /^.{5,}$/;
